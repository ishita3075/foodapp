spring.application.name=foodapp
# JWT settings (use a strong secret in real apps)
jwt.secret=replace_this_with_a_long_secret_string_of_at_least_32_chars!!!
jwt.expiration-ms=3600000
server.port=${PORT:8080}