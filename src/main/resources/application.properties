spring.application.name=foodapp

# JWT settings
jwt.secret=${JWT_SECRET:changeme}   # Will use Render's env variable
jwt.expiration-ms=3600000           # 1 hour expiry

# Use Render's dynamic PORT instead of hardcoding 8080
server.port=${PORT:8080}
